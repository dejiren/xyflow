import React, { PropsWithChildren } from 'react';
declare const ReactFlowProvider: {
    ({ children }: PropsWithChildren): React.JSX.Element;
    displayName: string;
};
export default ReactFlowProvider;
